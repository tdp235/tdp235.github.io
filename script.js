document.getElementById('sample-form').addEventListener('submit', function(event) {
    event.preventDefault(); // Prevent page reload
    alert('Sample video generation is in progress...');
    // In the future, you can add logic to display a generated video preview here.
});
